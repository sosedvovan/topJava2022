<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- To enable JMX Management -->
    <jmxConfigurator/>

    <appender name="file" class="ch.qos.logback.core.FileAppender">
        <file>${TOPJAVA_ROOT}/log/topjava.log</file>

        <encoder>
            <charset>UTF-8</charset>
            <pattern>%date %-5level %logger{50}.%M:%L - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{HH:mm:ss.SSS} %-5level %class{50}.%M:%L - %msg%n</pattern>
        </encoder>
    </appender>

<!--добавили чтобы sql запросы показывал в терминале когда тесты запускаем-->
<!--те когда на рантайме обращаются к классам из этой папки(к роуМапперу например)
    - будет сообщение от логера с уровнем "debug"-->
    <logger name="org.springframwork.jdbc" level="debug"/>

    <logger name="ru.javawebinar.topjava" level="debug"/>

<!--уровень был "info"  -->
    <root level="WARN">
        <appender-ref ref="file"/>
        <appender-ref ref="console"/>
    </root>
</configuration>
